export const GROUP_LOCKED_OPEN_CYCLE_REASON_CODE = 'GROUP_LOCKED_OPEN_CYCLE';

export const GROUP_LOCKED_OPEN_CYCLE_MESSAGE =
  'Group is locked while a cycle is open. Try again after the cycle closes.';

export const GROUP_RULESET_REQUIRED_REASON_CODE = 'GROUP_RULESET_REQUIRED';

export const GROUP_RULESET_REQUIRED_MESSAGE =
  'Group rules must be configured before this action is allowed.';
